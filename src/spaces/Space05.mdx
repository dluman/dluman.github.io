---
label: End state
---

import Space from '../components/Space.astro';

<Space
  index={4}
  label={frontmatter.label}
  headline="Know when to stop."
  cta="Start building"
  ctaHref="#"
  theme="space--coral"
>
Five spaces is a ceiling, not a target. The pattern works because it is
finite. Give the user an exit — a CTA, a link, a return to vertical flow.
</Space>

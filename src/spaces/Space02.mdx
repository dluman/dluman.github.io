---
label: Behavior
---

import Space from '../components/Space.astro';

<Space
  index={1}
  label={frontmatter.label}
  headline="Native scroll, forced axis."
  cta="See the code"
  ctaHref="#"
  theme="space--rose"
>
We use CSS scroll-snap and translate vertical wheel input into horizontal
motion. The result feels like a horizontal website, not a hijacked scroll.
</Space>
